# Development dependencies for ML Dev Environment
#
# Install with: pip install -r requirements-dev.txt

# Include base requirements
-r requirements.txt

# Code quality and formatting
pre-commit>=3.5.0,<4.0.0
black>=23.0.0,<25.0.0
ruff>=0.1.0,<1.0.0  # Fast linter and formatter (replaces flake8, isort, pylint)
isort>=5.12.0,<6.0.0  # Keep for compatibility

# Type checking
mypy>=1.7.0,<2.0.0
types-PyYAML>=6.0.0

# Testing (if tests are added in the future)
pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0
